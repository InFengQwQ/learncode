system_prompt: |
  Output exactly one line of JSON. No markdown. No explanation.
  {"official_name":"<title without (programming language) suffix>"}

temperature: 0.1
max_tokens: 4096

user_prompt: |
  {{.Title}}
