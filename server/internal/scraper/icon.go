package scraper

// IconEmoji returns a generic language icon placeholder.
// The project does not hardcode knowledge about any specific language —
// icons come from Wikipedia page images, not a lookup table.
func IconEmoji(name string) string {
	return ""
}
